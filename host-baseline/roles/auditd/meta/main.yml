---
galaxy_info:
  role_name: auditd
  namespace: michealzs
  author: Micheal
  description: >-
    auditd rules for privileged access: execve by real users, identity and sudoers
    changes, privileged commands, module loading, and the recording configuration.
  license: MIT
  min_ansible_version: "2.15"
  platforms:
    # EL8 needs python39 installed and ansible_python_interpreter pointed at it,
    # because its system Python 3.6 is below what ansible-core 2.17 targets.
    - name: EL
      versions:
        - "8"
        - "9"
  galaxy_tags:
    - auditd
    - audit
    - compliance
    - security

dependencies: []
