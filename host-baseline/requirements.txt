# Control node dependencies. Pinned, because an ansible-lint minor release can
# add a rule and turn a green pipeline red without a commit.
ansible-core==2.17.6
ansible-lint==24.10.0
molecule==24.9.0
molecule-plugins[docker]==23.5.3
docker==7.1.0
yamllint==1.35.1
